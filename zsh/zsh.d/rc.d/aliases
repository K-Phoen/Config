# des alias utiles
alias grep='grep --color=auto'
alias lh='ls -lh --color=auto'
alias ll='lh'
alias open='xdg-open'

# git
alias gst='git status'
alias gcm='git checkout master'
alias gpm='git pull --ff-only origin master'

function mkcd() {
  mkdir -p $@ && cd "${@: -1}"
}
